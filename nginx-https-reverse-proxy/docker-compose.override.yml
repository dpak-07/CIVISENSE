version: '3.8'
services:
  frontend:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
      - ai-service
    environment:
      - DOMAIN=civisence.duckdns.org
      - EMAIL=deepakofficial0103@gmail.com
    privileged: true
    cap_add:
      - NET_ADMIN

